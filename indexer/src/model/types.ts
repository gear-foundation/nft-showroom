export enum SaleStatus {
  Open = 'open',
  Sold = 'sold',
  Canceled = 'canceled',
}

export enum AuctionStatus {
  Open = 'open',
  Closed = 'closed',
  Canceled = 'canceled',
}

export enum OfferStatus {
  Open = 'open',
  Accepted = 'accepted',
  Canceled = 'canceled',
}
